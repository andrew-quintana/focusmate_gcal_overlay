{
  "name": "focusmate-calendar-extension",
  "version": "1.0.0",
  "description": "A Chrome extension for managing FocusMate calendar",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.3.3"
  }
}

